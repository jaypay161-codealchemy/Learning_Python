The folder is created to test the UV environment using a basic set of codes.

However, it stands successful after following below steps...

> Create a Folder
> Open terminal inside new folder created
> Write cmd line as "uv init myproject"
> myproject folder will be created with all necessary python files
> go inside myproject folder by writing cmd line "cd myproject"
> now write cmd line "uv run python main.py"
> output will show : 
